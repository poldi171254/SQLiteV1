if( BUILD_MYSQLE_COLLECTION )
    add_subdirectory( mysqlestorage )
endif()
if( BUILD_SQLITE_COLLECTION )
    add_subdirectory( sqlitestorage )
endif()
if( MySQL_FOUND )
    add_subdirectory( mysqlserverstorage )
endif()
